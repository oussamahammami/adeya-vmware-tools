Excute this command to run "adeya-vmware-tools" playbook

> ansible-playbook /etc/ansible/roles/adeya-vmware-tools/adeya-vmware-tools.yml -s -l serverXX.adeya.ch

; ! For more information about default vars please see defaults/main.yml : you can change these values by using "--extra-vars or -e"
;
; Example for serverXX.adeya.ch:
;
; -e VARS, --extra-vars=VARS
;     Extra variables to inject into a playbook
; -l SUBSET, --limit=SUBSET
;     Further limits the selected host/group patterns.
; -s, --sudo
;     Force all plays to use sudo, even if not marked as such.
; -i PATH, --inventory=PATH
;     The PATH to the inventory hosts file, which defaults to /etc/ansible/hosts.


